<%= form_for(@estadisticasconsulta) do |f| %>
  <% if @estadisticasconsulta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estadisticasconsulta.errors.count, "error") %> prohibited this estadisticasconsulta from being saved:</h2>

      <ul>
      <% @estadisticasconsulta.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br />
    <%= f.datetime_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :bazar_id %><br />
    <%= f.text_field :bazar_id %>
  </div>
  <div class="field">
    <%= f.label :consulta %><br />
    <%= f.text_field :consulta %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
